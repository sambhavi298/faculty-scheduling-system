# Implementation Status

Faculty Availability & Intelligent Appointment Scheduling System — status as of this backend milestone handoff. A component is only marked Complete if the code exists AND tests for it were actually executed and passed. See `docs/DEVELOPMENT_HANDOFF.md` for exact test commands and results.

| Component | Status | Notes |
|---|---|---|
| Database schema | Complete | Migrations 0001–0008 applied and verified from a clean database (see handoff doc). Core tables, `appointments` exclusion constraint, functions/triggers, views, materialized view, booking-time availability enforcement, `faculty_availability` overlap protection, database session timezone pinned to `Asia/Kolkata` (migration 0008 — see handoff doc's "Timezone: why this exists"). |
| AppointmentRepository | Complete | Booking, idempotent retries, guarded transitions (approve/reject/cancel/complete/markMissed), error mapping. 100% statement/line/function coverage, 94.73% branch coverage — one untested branch (`appointment.repository.ts:76`): a conflict error with a `clientRequestId` present, but the idempotency lookup finding no matching row. Pre-existing, not introduced by this milestone. |
| AppointmentService | Complete | Ownership checks, idempotent no-ops, state-machine validation, orchestration for all six transitions. 100% coverage across statements/branches/functions/lines. |
| State Machine | Complete | Pure domain object, mirrors the database trigger's transition rules exactly. 100% coverage. |
| Availability (faculty_availability, teaching schedule, leave/blocked exceptions) | Partial | Database-level computation (`is_faculty_available()`, `get_available_slots()`) and overlap protection are complete and tested. No application-layer Repository/Service yet, and no HTTP endpoint (`GET /api/faculty/:facultyId/availability` etc.) — deliberately deferred out of the Phase 2 HTTP pass, since there is no Service to wrap yet. Faculty still cannot manage their own availability through any code path. |
| Notifications | Not started | `notifications` table exists in the schema. No `NotificationService`, nothing writes to the table, nothing dispatches an email/push/websocket notification. |
| Controllers / API | Complete (Appointment Management module only) | Phase 2: `src/app.ts`, `src/controllers/appointment.controller.ts`, `src/routes/appointment.routes.ts` — Express + TypeScript, thin Controllers with no SQL/business logic, wrapping the already-tested `AppointmentService`. Covers every Appointment Management endpoint from the Level 5 API Contract Table: `POST /api/appointments`, `GET /api/appointments/mine`, `GET /api/appointments/pending`, `PATCH /api/appointments/:id/{approve,reject,cancel,complete,missed}`. Domain errors mapped to HTTP status codes centrally in `src/middleware/error-handler.middleware.ts` (400 ValidationError, 404 NotFoundError, 409 SlotConflictError/FacultyUnavailableError/AlreadyProcessedError/InvalidTransitionError). Faculty Availability and admin/reporting endpoints are not built — see the Availability row above and `docs/GITHUB_ISSUES.md`. 21 new HTTP tests (`tests/http/appointment.http.test.ts`, real Express app + real PostgreSQL via `supertest`) plus 2 new unit tests for the two trivial `AppointmentService` listing passthroughs added to support this layer. |
| Authentication | Not started (temporary stand-in in place) | No login, session, or token verification still exists. `src/middleware/identify.middleware.ts` reads `X-User-Id`/`X-User-Role` headers supplied directly by the client with no cryptographic verification, purely so every Controller has a consistent `req.user` to read and so a missing identity is a clean 401 rather than a crash. This does not weaken the pre-existing trust boundary — `AppointmentService`'s ownership checks already trusted a caller-supplied id "on faith" before this HTTP layer existed — it only makes that boundary reachable over HTTP. Task 2 in the roadmap (below) replaces this middleware's implementation with real verification; no other file should need to change. |
| Student Frontend | Not started | Assigned to Rekha — see `docs/GITHUB_ISSUES.md`. |
| Faculty Frontend | Not started | Assigned to Sankalp — see `docs/GITHUB_ISSUES.md`. |
| Admin Frontend | Not started | Assigned to Mohammed Izhaan — see `docs/GITHUB_ISSUES.md`. |
| Testing | Substantial | 223 tests actually executed and passing, with genuinely successful process exit codes verified (not just printed pass counts): 211 (unit/integration/concurrency/security/advanced-SQL/HTTP, `npm test`) + 5 (performance, real measurements) + 7 (failure injection, real PostgreSQL stop/restart, run in isolation). Fully verified against a database session pinned to `Asia/Kolkata` (migration 0008) on PostgreSQL 16 — see handoff doc's "Expected test result" for the full history, including a real concurrency-correctness gap (PostgreSQL deadlock outcomes, SQLSTATE 40P01, not previously mapped to SlotConflictError) found by deliberately stress-testing the flagship double-booking test 15+ times rather than trusting one clean run, and an index-choice test-design bug found from an actual PostgreSQL 18 failure. The pre-existing 200 were independently re-confirmed on Windows/PostgreSQL 18; the 23 new HTTP-layer tests added in Phase 2 have NOT yet been re-run there. No frontend tests exist yet, since no frontend exists yet. **A second, so-far-unfixed instance of the same 40P01-vs-23P01 deadlock class was observed once during this pass**, in `tests/integration/faculty-availability-overlap-protection.test.ts` (unrelated to the HTTP layer — it hits the `faculty_availability` GiST exclusion constraint directly, with no application code involved) — see handoff doc's "Known open issue" for detail; it was not fixed as part of Phase 2 since it's outside that scope. |
| Deployment | Not started | No deployment configuration, no CI pipeline, no hosting setup. Out of scope for this milestone. |
